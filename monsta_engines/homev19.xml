<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:css='false' b:defaultmessages='false' b:layoutsVersion='3' b:responsive='true' xmlns='http://www.w3.org/1999/xhtml' xmlns:b='http://www.google.com/2005/gml/b' xmlns:data='http://www.google.com/2005/gml/data' xmlns:expr='http://www.google.com/2005/gml/expr'>
<head>
    <title>MonstaComics V4.0</title>
    <meta content='width=device-width, initial-scale=1, maximum-scale=1' name='viewport'/>
    <link href='https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&amp;display=swap' rel='stylesheet'/>
    
    <b:skin><![CDATA[
        /* --- MONSTA V4.0 CORE THEME --- */
        :root {
            --bg: #0b0b0b;
            --card: #121212;
            --m-green: #00ff88;  /* Green Monster */
            --m-purple: #a349f5; /* Purple Glow */
            --text: #ffffff;
        }

        body { background-color: var(--bg); color: var(--text); font-family: 'Inter', sans-serif; margin: 0; padding: 0; }
        header { background: #111; padding: 15px 5%; display: flex; align-items: center; border-bottom: 1px solid #222; position: sticky; top: 0; z-index: 1000; }
        .logo { font-weight: 900; letter-spacing: 2px; font-size: 20px; cursor: pointer; text-transform: uppercase; }
        .logo span { color: var(--m-purple); }

        .container { max-width: 1200px; margin: 0 auto; padding: 25px; }
        .manga-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(165px, 1fr)); gap: 25px; }

        /* THE MONSTER CARD */
        .manga-card {
            background: var(--card); border: 1.5px solid var(--m-green);
            box-shadow: 0 4px 12px rgba(163, 73, 245, 0.2);
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            cursor: pointer; position: relative; overflow: hidden;
        }
        .manga-card:hover { transform: translateY(-8px); box-shadow: 0 12px 30px rgba(163, 73, 245, 0.6); border-color: var(--m-green); }
        .manga-card img { width: 100%; aspect-ratio: 3/4; object-fit: cover; display: block; transition: 0.3s; }
        .manga-card:hover img { filter: brightness(1.2); }
        
        .card-info { padding: 12px; background: #111; border-top: 1px solid #222; }
        .m-title { font-size: 13px; font-weight: 700; margin-bottom: 5px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; color: #fff; }
        .m-meta { display: flex; justify-content: space-between; align-items: center; }
        .m-rating { color: var(--m-green); font-weight: 900; font-size: 11px; }

        /* SKELETON */
        .skeleton { background: #1a1a1a; height: 250px; border: 1px solid #222; position: relative; overflow: hidden; }
        .skeleton::after { content: ""; position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(90deg, transparent, rgba(255,255,255,0.05), transparent); animation: slide 1.5s infinite; }
        @keyframes slide { 0% { transform: translateX(-100%); } 100% { transform: translateX(100%); } }

        /* READER */
        #reader-view { display: none; background: #000; min-height: 100vh; }
        .reader-nav { position: sticky; top: 0; background: rgba(11,11,11,0.95); padding: 15px; border-bottom: 1px solid var(--m-green); display: flex; align-items: center; z-index: 1100; }
        .btn-back { background: var(--m-purple); color: #fff; border: none; padding: 8px 20px; font-weight: bold; cursor: pointer; border-radius: 4px; }
        .img-stack img { width: 100%; max-width: 800px; display: block; margin: 0 auto; }

        .hidden { display: none !important; }
        @media (max-width: 480px) { .manga-grid { grid-template-columns: repeat(2, 1fr); gap: 12px; } }
    ]]></b:skin>
</head>
<body>
    <header><div class='logo' onclick='window.location.reload()'>MONSTA<span>COMICS</span></div></header>

    <div class='container' id='home-view'>
        <div class='manga-grid' id='main-grid'>
            <div class='skeleton'/><div class='skeleton'/><div class='skeleton'/><div class='skeleton'/>
        </div>
    </div>

    <div id='reader-view'>
        <div class='reader-nav'>
            <button class='btn-back' onclick='window.location.reload()'>KEMBALI</button>
            <div id='nav-title' style='margin-left:20px; font-weight:bold; color:var(--m-green);'/>
        </div>
        <div class='img-stack' id='reader-imgs'/>
    </div>

    <div class='hidden'><b:section id='main' showaddelement='yes'/></div>

    <script type='text/javascript'>
    // <![CDATA[
        // Link Sakti Bos (Pastikan link ini masih aktif di browser)
        const RAW_URL = "https://cdn.discordapp.com/attachments/1468625981584703673/1468929555380043830/monsta_index.json?ex=6985cec1&is=69847d41&hm=dab16bd3285627383c932252854554714c3eeb53acbb89a3ba3a2ab247085464&";

        // Menggunakan jalur proxy yang lebih stabil untuk data JSON mentah
        const getUrl = (url) => `https://api.allorigins.win/raw?url=${encodeURIComponent(url)}`;

        async function init() {
            const grid = document.getElementById('main-grid');
            try {
                const res = await fetch(getUrl(RAW_URL));
                if(!res.ok) throw new Error();
                const data = await res.json();
                
                grid.innerHTML = data.map(m => `
                    <div class="manga-card" onclick="openManga('${m.cdn}')">
                        <img src="${m.cover}" onerror="this.src='https://via.placeholder.com/300x400?text=No+Cover'"/>
                        <div class="card-info">
                            <div class="m-title">${m.title}</div>
                            <div class="m-meta">
                                <span class="m-rating">★ ${m.rating}</span>
                                <span style="font-size:9px; color:var(--m-purple); border:1px solid var(--m-purple); padding:1px 4px; font-weight:bold;">${m.type}</span>
                            </div>
                        </div>
                    </div>
                `).join('');
            } catch (e) {
                grid.innerHTML = `
                    <div style="grid-column:1/-1; text-align:center; padding:50px;">
                        <p style="color:var(--m-purple); font-weight:bold;">SUMMONING FAILED</p>
                        <p style="font-size:12px; color:#666;">Link Discord kedaluwarsa atau Proxy sedang sibuk.</p>
                        <button onclick="location.reload()" style="margin-top:20px; background:var(--m-green); color:#000; border:none; padding:10px 20px; font-weight:bold; cursor:pointer;">TRY AGAIN</button>
                    </div>`;
            }
        }

        async function openManga(cdnUrl) {
            document.getElementById('home-view').style.display = 'none';
            document.getElementById('reader-view').style.display = 'block';
            window.scrollTo(0,0);
            
            const imgArea = document.getElementById('reader-imgs');
            imgArea.innerHTML = "<div class='skeleton' style='height:100vh; width:100%'></div>";

            try {
                const res = await fetch(getUrl(cdnUrl));
                const manga = await res.json();
                document.getElementById('nav-title').innerText = manga.title;
                
                // Chapter 0 adalah yang terbaru
                const images = manga.chapters[0].images;
                imgArea.innerHTML = images.map(i => `<img src="${i}" loading="lazy"/>`).join('');
            } catch (e) {
                alert("Gagal memuat chapter. Cek link Discord detail komik.");
            }
        }

        window.onload = init;
    // ]]>
    </script>
</body>
</html>